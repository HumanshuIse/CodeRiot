# Server & Framework
fastapi==0.111.0
uvicorn==0.30.1

# Data Validation & Settings
pydantic==2.7.4
pydantic-settings==2.3.4  # For managing environment variables

# Database & ORM
sqlalchemy==2.0.30
psycopg2-binary==2.9.9  # Driver for PostgreSQL
alembic==1.13.1          # For database migrations
asyncpg==0.29.0           # Recommended for async SQLAlchemy with PostgreSQL

# Authentication & Security
passlib[bcrypt]==1.7.4   # Use [bcrypt] to ensure the hashing dependency is installed
python-jose[jwt]==3.3.0  # For JWT handling
Authlib==1.3.0           # For Google OAuth2 integration

# Caching & Message Queue
redis==5.0.7             # Python client for your Redis matchmaking queue

# HTTP Clients
httpx==0.27.0            # For making async requests (to the judge server)
requests==2.32.3         # For making sync requests (to the Ollama hint service)

# Code Execution Engine
docker==7.1.0

# Email & Background Tasks
fastapi-mail==1.4.1      # For sending emails (e.g., registration, password reset)

# Configuration
python-dotenv==1.0.1     # For loading .env files

# Utilities
python-multipart==0.0.9  # Required for form data (e.g., OAuth2PasswordRequestForm)
pytz==2024.1             # For timezone handling

# --- Development Tools ---
# (Optional but highly recommended)
black==24.4.2            # The uncompromising code formatter
flake8==7.1.0            # Linter for code style and errors